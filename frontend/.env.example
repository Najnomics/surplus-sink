# Copy to frontend/.env.local only if you need to override RPC.
VITE_RPC_URL=https://sepolia.unichain.org
# Signing key comes from the repo-root `.env` `PRIVATE_KEY` during `npm run dev`.
# Never set VITE_PRIVATE_KEY on Vercel.
