# Copy to frontend/.env.local for a live-network build.
# Overrides the RPC the console reads from (defaults to the chain's public RPC).
# Only used when deployed.json points at a non-local chain (Unichain 130 / 1301).
VITE_RPC_URL=https://sepolia.unichain.org
